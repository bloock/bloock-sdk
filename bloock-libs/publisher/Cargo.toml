[package]
name = "bloock-publisher"
version = "0.1.0"
edition = "2021"

[lib]
bench = false

[dependencies]
thiserror = "1.0.31"
serde = { version = "1.0.123", features = ["std", "derive"] }

[features]
testing = []