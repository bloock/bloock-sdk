name: Test Workflow
on:
  push:
    branches:
      - feature/EIT-3075

jobs:
  test_build:
    uses: ./.github/workflows/build_rust.yml
    strategy:
      fail-fast: false
      matrix:
        platform:
          - os_name: FreeBSD-x86_64
            os: ubuntu-latest
            target: x86_64-unknown-freebsd
            bin: libbloock_bridge.so
            name: bloock-FreeBSD-x86_64.tar.gz
            cargo_command: cross

          - os_name: Linux-x86_64-gnu
            os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            bin: libbloock_bridge.so
            name: bloock-Linux-x86_64-gnu.tar.gz
            cargo_command: cargo
          
          - os_name: Linux-x86_64-musl
            os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            bin: libbloock_bridge.so
            name: bloock-Linux-x86_64-musl.tar.gz
            cargo_command: cross

          - os_name: Windows-aarch64
            os: windows-latest
            target: aarch64-pc-windows-msvc
            bin: bloock_bridge.dll
            name: bloock-Windows-aarch64.zip
            cargo_command: cargo

          - os_name: macOS-x86_64
            os: macOS-latest
            target: x86_64-apple-darwin
            bin: libbloock_bridge.dylib
            name: bloock-Darwin-x86_64.tar.gz
            cargo_command: cargo

          - os_name: macOS-aarch64
            os: macOS-latest
            target: aarch64-apple-darwin
            bin: libbloock_bridge.dylib
            name: bloock-Darwin-aarch64.tar.gz
            cargo_command: cargo
    with:
      os_name: ${{ matrix.platform.os_name }}
      os: ${{ matrix.platform.os }}
      target: ${{ matrix.platform.target }}
      bin: ${{ matrix.platform.bin }}
      name: ${{ matrix.platform.name }}
      cargo_command: ${{ matrix.platform.cargo_command }}
